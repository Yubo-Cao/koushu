node_modules
.next
out
dist
target
src-tauri/target
src-tauri/gen
*.log
.DS_Store
tsconfig.tsbuildinfo

# Local credentials — never commit
openrouter
*.key
.env*
!.env.example

# Regenerate with scripts/gen-swift-bindings.sh — checking these in would
# let them drift from the Rust they are generated from without anyone noticing.
core/generated/
